:root {
    --primary-bg: #0d7b87;
    --border-color: #ccc;
    --hover-color: #007BFF;
    --text-color: #333;
  }
  
  /* Cabeçalho fixo no topo com layout flexível */
  header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-around;
    padding: 10px 50px;
    border-bottom: 1px solid var(--border-color);
    background-color: var(--primary-bg);
    z-index: 999; /* Garante que fique acima de outros elementos */
  }
  
  /* Logo com alinhamento central */
  .logo {
    display: flex;
    align-items: center;
  }
  
  .logo img {
    max-height: 90px;
  }
  
  /* Linha divisória entre logo e navegação */
  .divider {
    border-left: 3px solid var(--border-color);
    height: 50px;
    margin-left: 20px;
    margin-right: 5px;
    align-self: center;

    
  }
  
  /* Links de navegação */
  .nav-links {
    display: flex;
    gap: 20px;
    margin-left: 600px;
  }
  
  .nav-links a {
    text-decoration: none;
    color: var(--text-color);
    font-weight: bold;
    font-size: 16px;
    padding: 20px;
    transition: color 0.3s ease;
    color: white;
  }
  
  .nav-links a:hover {
    color: var(--hover-color);
  }
  









   <style>
    /* Esconde o menu lateral em telas grandes */
    .mobile-sidebar {
        display: none;
    }
    @media (max-width: 768px) {
        header {
            display: none;
        }
        .mobile-sidebar {
            display: block;
            position: relative;
            width: 100%;
            background: #fff;
            z-index: 1000;
        }
        .mobile-header {
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            padding: 10px 0;
        }
        .mobile-logo {
            margin: 0 auto;
            display: block;
            height: 40px;
        }
        .menu-toggle {
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            font-size: 2rem;
            cursor: pointer;
        }
        .mobile-sidebar-menu {
            position: fixed;
            top: 0;
            left: -220px;
            width: 200px;
            height: 100%;
            background: #fff;
            box-shadow: 2px 0 8px rgba(0,0,0,0.1);
            display: flex;
            flex-direction: column;
            padding-top: 60px;
            transition: left 0.3s;
            z-index: 1100;
        }
        .mobile-sidebar-menu.open {
            left: 0;
        }
        .mobile-sidebar-menu a {
            padding: 15px 25px;
            text-decoration: none;
            color: #333;
            border-bottom: 1px solid #eee;
            font-weight: bold;
        }
        .mobile-sidebar-menu a:last-child {
            border-bottom: none;
        }
    }
    </style>